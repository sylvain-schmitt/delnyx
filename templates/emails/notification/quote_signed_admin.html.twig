{% extends 'emails/layout/base.html.twig' %}

{% block body %}
	<h1>✍️ Devis Signé !</h1>

	<p>Bonne nouvelle ! Un client vient de signer un devis via son espace client.</p>

	<div class="info-box" style="background-color: #f0fdf4; padding: 20px; border-radius: 8px; border: 1px solid #bbf7d0; margin: 20px 0;">
		<p style="margin: 5px 0;">
			<strong>📄 Devis :</strong>
			{{ quote.numero }}</p>
		<p style="margin: 5px 0;">
			<strong>👤 Client :</strong>
			{{ client.nomComplet }}</p>
		<p style="margin: 5px 0;">
			<strong>💰 Montant :</strong>
			{{ quote.montantTTC|number_format(2, ',', ' ') }}
			€ TTC</p>
	</div>

	<div style="text-align: center; margin: 30px 0;">
		<a href="{{ url('admin_quote_show', {id: quote.id}) }}" style="display: inline-block; padding: 12px 24px; background-color: #22c55e; color: white; text-decoration: none; border-radius: 6px; font-weight: 600;">
			Voir le devis
		</a>
	</div>

	<p style="color: #64748b; font-size: 14px;">
		Le processus automatisé (facturation/acompte) a été lancé si configuré.
	</p>
{% endblock %}
