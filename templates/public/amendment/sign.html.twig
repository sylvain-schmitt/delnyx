{% extends 'public/layout/base.html.twig' %}

{% block title %}Signer l'avenant {{ amendment.numero }} - Delnyx{% endblock %}

{% block body %}
<div class="max-w-2xl mx-auto">
    <div class="bg-white/10 backdrop-blur-xl border border-white/20 rounded-xl p-8">
        <h1 class="text-2xl font-bold text-white mb-6">Signature de l'avenant</h1>
        
        <div class="mb-6">
            <p class="text-white/80 mb-2">
                <span class="font-medium text-white">Avenant :</span> {{ amendment.numero }}
            </p>
            <p class="text-white/80 mb-2">
                <span class="font-medium text-white">Montant TTC :</span> {{ amendment.montantTTCFormate }}
            </p>
            {% if amendment.quote %}
                <p class="text-white/80">
                    <span class="font-medium text-white">Devis associé :</span> {{ amendment.quote.numero }}
                </p>
            {% endif %}
        </div>

        <div class="bg-blue-500/20 border border-blue-500/30 rounded-lg p-4 mb-6">
            <p class="text-blue-300 text-sm">
                {{ ux_icon('lucide:info', {class: 'w-4 h-4 inline mr-2'}) }}
                En signant cet avenant, vous acceptez les modifications apportées au devis initial.
            </p>
        </div>

        <form method="post" class="space-y-6">
            <div>
                <label for="signature_name" class="block text-sm font-medium text-white mb-2">
                    Votre nom complet *
                </label>
                <input 
                    type="text" 
                    id="signature_name" 
                    name="signature_name" 
                    required
                    class="w-full px-4 py-2 bg-white/5 border border-white/20 rounded-lg text-white placeholder-white/40 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    placeholder="Ex: Jean Dupont"
                >
            </div>

            <div class="flex items-start">
                <input type="checkbox" id="accept_terms" name="accept_terms" required
                    class="mt-1 w-4 h-4 rounded border-white/20 bg-white/5 text-blue-500 focus:ring-2 focus:ring-blue-500">
                <label for="accept_terms" class="ml-3 text-sm text-white/80">
                    Je certifie avoir lu et accepté cet avenant. *
                </label>
            </div>

            <div class="flex gap-4">
                <button type="submit" class="btn-primary flex-1 py-3">
                    {{ ux_icon('lucide:pen-line', {class: 'w-5 h-5 inline mr-2'}) }}
                    Signer l'avenant
                </button>
                <a href="{{ path('public_amendment_view', {
                    id: amendment.id,
                    expires: app.request.query.get('expires'),
                    signature: app.request.query.get('signature')
                }) }}" class="btn-secondary flex-1 py-3 text-center">
                    Annuler
                </a>
            </div>
        </form>
    </div>
</div>
{% endblock %}


