<header class="fixed top-0 left-0 right-0 z-50 bg-background/95 backdrop-blur-sm">
	<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
		<div
			class="flex justify-between items-center h-16">

			<!-- Logo -->
			<div class="flex-shrink-0">
				<a href="{{ path('app_home') }}" class="flex items-center space-x-2">
					<img src="{{ asset('images/logo-delnyx.svg') }}" alt="Delnyx" class="h-6 sm:h-8 lg:h-10 w-auto transition-all duration-300">
				</a>
			</div>

			<!-- Desktop Navigation -->
			<nav class="hidden md:flex space-x-8">
				<a href="{{ path('app_home') }}" class="nav-link px-3 py-2 hover:text-blue-500 transition {{ app.request.attributes.get('_route') == 'app_home' ? 'border-b-2 border-blue-500 text-blue-600' : 'text-foreground' }}">
					Accueil
				</a>
				<a href="{{ path('app_about') }}" class="nav-link px-3 py-2 hover:text-blue-500 transition {{ app.request.attributes.get('_route') == 'app_about' ? 'border-b-2 border-blue-500 text-blue-600' : 'text-foreground' }}">
					À propos
				</a>
				<a href="{{ path('app_services') }}" class="nav-link px-3 py-2 hover:text-blue-500 transition {{ app.request.attributes.get('_route') == 'app_services' ? 'border-b-2  border-blue-500 text-blue-600' : 'text-foreground' }}">
					Services
				</a>
				<a href="{{ path('app_portfolio') }}" class="nav-link px-3 py-2 hover:text-blue-500 transition {{ app.request.attributes.get('_route') == 'app_portfolio' ? 'border-b-2 border-blue-500 text-blue-600' : 'text-foreground' }}">
					Portfolio
				</a>
				<a href="{{ path('app_contact') }}" class="nav-link px-3 py-2 hover:text-blue-500 transition {{ app.request.attributes.get('_route') == 'app_contact' ? 'border-b-2 border-blue-500 text-blue-600' : 'text-foreground' }}">
					Contact
				</a>
			</nav>


			<!-- Mobile menu button -->
			<div class="md:hidden">
				<button
					id="menu-toggle" class="p-2 rounded-md text-foreground hover:text-primary focus:outline-none">
					<!-- Icône Menu -->
					<svg id="icon-menu" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewbox="0 0 24 24" stroke="currentColor" stroke-width="2">
						<path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
					</svg>
					<!-- Icône Close -->
					<svg id="icon-close" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 hidden" fill="none" viewbox="0 0 24 24" stroke="currentColor" stroke-width="2">
						<path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/>
					</svg>
				</button>
			</div>
		</div>
	</div>

	<!-- Mobile Navigation -->
	<div id="mobile-menu" class="overflow-hidden max-h-0 md:hidden bg-background transition-all duration-300 ease-in-out">
		<div class="px-2 pt-2 pb-3 space-y-1">
			<a href="{{ path('app_home') }}" class="block px-3 py-2 transition-colors duration-300 text-center hover:text-blue-500
					           {{ app.request.attributes.get('_route') == 'app_home' ? ' text-blue-600' : 'text-foreground' }}">
				Accueil
			</a>
			<a href="{{ path('app_about') }}" class="block px-3 py-2 transition-colors duration-300 text-center hover:text-blue-500
					           {{ app.request.attributes.get('_route') == 'app_about' ? ' text-blue-600' : 'text-foreground' }}">
				À propos
			</a>
			<a href="{{ path('app_services') }}" class="block px-3 py-2 transition-colors duration-300 text-center hover:text-blue-500
					           {{ app.request.attributes.get('_route') == 'app_services' ? ' text-blue-600' : 'text-foreground' }}">
				Services
			</a>
			<a href="{{ path('app_portfolio') }}" class="block px-3 py-2 transition-colors duration-300 text-center hover:text-blue-500
					           {{ app.request.attributes.get('_route') == 'app_portfolio' ? ' text-blue-600' : 'text-foreground' }}">
				Portfolio
			</a>
			<a href="{{ path('app_contact') }}" class="block px-3 py-2 transition-colors duration-300 text-center hover:text-blue-500
					           {{ app.request.attributes.get('_route') == 'app_contact' ? ' text-blue-600' : 'text-foreground' }}">
				Contact
			</a>
		</div>
	</div>

</header>
